   public enum Tools {
        Sword,
        Brick,
        Phone,
        Tetromino
   }

public enum SceneType
{
    MapUp,MapDown,Menu,Dialogue
}

public enum PersistentType
{
    ReadWrite,DontPersist
}

